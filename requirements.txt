cryptography
aiohttp>=3.9.5  # not directly required, pinned by Snyk to avoid a vulnerability
urllib3>=2.2.2  # not directly required, pinned by Snyk to avoid a vulnerability

# Web packages
Flask
Jinja2
werkzeug>=3.0.1  # not directly required, pinned by Snyk to avoid a vulnerability
aiohttp>=3.9.4  # not directly required, pinned by Snyk to avoid a vulnerability

# Translation package
googletrans==3.1.0a0

# Web Scraping packages
beautifulsoup4
bs4
crawler-user-agents

# Image Processing package
opencv-python
numpy>=2.0.0  # not directly required, pinned by Snyk to avoid a vulnerability

# DNS package
dnspython>=2.6.1  # not directly required, pinned by Snyk to avoid a vulnerability

# Geolocation package
geoip2>=4.8.0

# Captcha package
captcha